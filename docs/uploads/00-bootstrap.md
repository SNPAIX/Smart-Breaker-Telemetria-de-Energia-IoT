# Etapa 0 — Bootstrap

- Monorepo: `backend-server/`, `front/`, `mobile/`, `ino/`, `docs/`.
- Rama de trabajo: `experiment/voltguard-platform-v2` (nunca `main`).
- CI actualizado a la nueva ruta/rama.
- Commit: `3c1586b`.
